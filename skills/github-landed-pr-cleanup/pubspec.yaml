name: github_landed_pr_cleanup
description: Automated discovery, local Git worktree cleanup, and Jetski attribution for landed GitHub PRs.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.12.0

dependencies:
  args: ^2.4.0
  path: ^1.9.0
